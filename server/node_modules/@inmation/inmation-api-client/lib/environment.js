'use strict';

let WS = null;
if (typeof (WebSocket) === "function") {
    // Browsers build-in WebSocket.
    WS = WebSocket // eslint-disable-line no-undef
}
else {
    // Node.js package 'ws'.
    WS = require('ws'); // eslint-disable-line global-require
}

module.exports = {
    WS: WS
}